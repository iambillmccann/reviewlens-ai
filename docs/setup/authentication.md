# Authentication Status

Reviewlens-ai currently does not require authentication.

Local and cloud development do not require identity-provider credentials or auth-specific environment variables.

If authentication is introduced in a future milestone, this document should be replaced with provider-agnostic setup instructions and environment-specific configuration guidance.
